name: Upload to S3
on:
  push:
    branches:
      - master
jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Set up AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1
    - name: Upload files to S3
      uses: aws-actions/aws-s3@v2
      with:
        args: sync ./vectra.txt s3://${{ secrets.AWS_S3_BUCKET }}
# name: Upload Repo to S3

# on:
#   push:
#     branches: master
      

# jobs:
#   deploy:
#     runs-on: windows-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v2
      
#       - name: Setup Python in the github server
#         uses: actions/setup-python@v2
#         with:
#           python-version: '3.7'
          
#       - name: Install AWS CLI in github server
#         run: |
#           pip install awscli
      
#       - name: Configure AWS credentials
#         run: |
#           aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws configure set default.region ap-south-1
      
#       - name: Sync files to S3 bucket
#         # aws s3 sync . s3://${{ secrets.AWS_S3_BUCKET }} --acl public-read --delete 
#         run: |
#            sync . s3://${{ secrets.AWS_S3_BUCKET }} 
